---
interface Props {
	id?: string;
	class?: string;
}

const { id, class: className } = Astro.props;
---

<section
	id={id}
	class:list={[
		"border-b border-neutral-400 dark:border-neutral-700 px-4 py-12 sm:px-8 sm:pt-16 sm:pb-32",
		className,
	]}
>
	<slot />
</section>
